all:
	cd src && make
	cd example && make

clean:
	cd src && make clean
	cd example && make clean
	rm -f lib/*
	rm -f libD/*

clobber:
	cd src && make clobber
	cd example && make clobber
	rm -f lib/*
	rm -f libD/*
